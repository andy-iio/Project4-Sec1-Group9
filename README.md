To install the requirements (so we all have the same versions of everything) run: `pip install -r requirements.txt` in your venv

NOTE: dont push any of your venv or pycache files to this repository!!!  
